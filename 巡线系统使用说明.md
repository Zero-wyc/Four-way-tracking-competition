# 四路巡线系统使用说明

## 一、系统概述

本巡线系统采用**加权位置估算 + 自适应PID控制**算法，具有以下特点：

- **查表法位置估算**：16种传感器状态直接查表，无浮点运算
- **自适应PID**：根据误差大小自动切换PID参数
- **速度平滑**：防止电机速度突变，保护电机
- **脱线恢复**：智能弧线搜索，快速找回黑线
- **标记识别**：支持5x5、15x15、40x40等不同尺寸标记

## 二、文件结构

```
src/
├── z_tracking.h      # 巡线系统配置文件（所有参数在此调整）
├── z_tracking.c      # 巡线系统核心实现
├── z_sensor.c        # 已集成新巡线模块（AI_xunji_moshi调用tracking_update）
└── z_sensor.h        # 传感器接口（无需修改）
```

## 三、快速开始

### 1. 编译项目

新添加的文件 `z_tracking.c` 需要添加到Keil工程中：
1. 在Keil中右键点击Project窗口的`src`文件夹
2. 选择"Add Existing Files to Group 'src'..."
3. 选择 `z_tracking.c` 添加

### 2. 系统初始化

在 `main()` 函数中，`setup_sensor()` 会自动调用 `tracking_init()` 完成初始化。

### 3. 启动巡线

通过串口发送命令切换AI模式：
```
$SMODE0!    # 切换到巡线模式（AI_mode = 0）
```

或在代码中设置：
```c
AI_mode = 0;  // 0 = 巡线模式
```

## 四、参数调优指南

所有参数都在 `z_tracking.h` 中定义，根据实际赛道情况调整。

### 4.1 PID参数调整

```c
// 小误差状态（|err| < 5）：高速直行
#define PID_KP_SMALL_ERR        25      // 比例系数 2.5
#define PID_KD_SMALL_ERR        80      // 微分系数 8.0

// 中等误差状态（5 ≤ |err| < 15）：弯道行驶
#define PID_KP_MID_ERR          35      // 比例系数 3.5
#define PID_KD_MID_ERR          60      // 微分系数 6.0

// 大误差状态（|err| ≥ 15）：急转弯
#define PID_KP_LARGE_ERR        50      // 比例系数 5.0
#define PID_KD_LARGE_ERR        40      // 微分系数 4.0
```

**调整建议：**

| 现象 | 调整方法 |
|------|----------|
| 走直线蛇形摆动 | 减小Kp，增大Kd |
| 转弯反应慢 | 增大Kp |
| 转弯后回正慢 | 增大Kd |
| 弯道冲出黑线 | 增大Kp，减小基础速度 |
| 直线加速慢 | 增大直行速度，减小Kp |

### 4.2 速度参数调整

```c
#define SPEED_STRAIGHT          25      // 直行速度（小误差时）
#define SPEED_CURVE             18      // 弯道速度（中等误差时）
#define SPEED_SLOW              12      // 慢速（大误差时）
```

**调整建议：**
- 直道多：增大 `SPEED_STRAIGHT`
- 弯道多：减小 `SPEED_CURVE`，增大PID参数
- 急弯多：减小 `SPEED_SLOW`，增大 `PID_KP_LARGE_ERR`

### 4.3 脱线恢复参数

```c
#define LOST_THRESHOLD          5       // 连续多少次全白确认脱线
#define LOST_RECOVERY_TIME      800     // 脱线恢复超时时间(ms)
```

**调整建议：**
- 车速快：减小 `LOST_THRESHOLD`，快速进入恢复模式
- 车速慢：增大 `LOST_THRESHOLD`，避免误判

### 4.4 速度平滑参数

```c
#define SPEED_MAX_CHANGE        3       // 每周期最大速度变化量
```

**调整建议：**
- 电机响应慢：减小此值
- 需要快速响应：增大此值（但可能产生抖动）

## 五、调试方法

### 5.1 串口调试输出

在 `loop_sensor()` 中添加调试输出：
```c
void loop_sensor(void) {
    static u32 debug_timer = 0;
    
    // ... 原有代码 ...
    
    // 每100ms输出一次调试信息
    if(millis() - debug_timer > 100) {
        debug_timer = millis();
        tracking_print_debug();
    }
}
```

输出格式：
```
S:1010 Pos:0 PID:5 L:20 R:20 OK
S:1001 Pos:20 PID:-25 L:40 R:10 OK
S:1111 Pos:99 PID:0 L:0 R:0 LOST
```

### 5.2 运行测试

在初始化后调用测试函数：
```c
void setup_sensor(void) {
    setup_xunji();
    setup_sound();
    setup_csb();
    setup_yssb();
    tracking_init();
    
    // 运行自检测试
    tracking_run_tests();
}
```

### 5.3 运行时调整参数

可以通过串口命令动态调整参数（需要在 `parse_cmd` 中添加支持）：
```c
// 示例：添加命令解析
if(strstr((char*)cmd, "TRACK_PID")) {
    int kp, ki, kd;
    sscanf((char*)cmd, "TRACK_PID:%d,%d,%d", &kp, &ki, &kd);
    tracking_set_pid(kp, ki, kd);
}
```

## 六、位置查表说明

传感器布局（从车后向前看）：
```
    车前方
      ↑
  ┌─────────┐
  │ s1  s2  s3  s4 │   ← 传感器
  │ 左外 左内 右内 右外 │
  └─────────┘
```

位置值定义（×10放大）：
| 传感器状态 (s1s2s3s4) | 位置值 | 说明 |
|----------------------|--------|------|
| 0110 | 0 | 居中 |
| 0100 | -12 | 偏左（左内检测到） |
| 0010 | 12 | 偏右（右内检测到） |
| 1000 | -30 | 最左（左外检测到） |
| 0001 | 30 | 最右（右外检测到） |
| 0000 | 100 | 全黑（标记点） |
| 1111 | 99 | 全白（脱线） |

## 七、常见问题排查

### 7.1 小车不走直线

1. 检查电机安装是否对称
2. 调整 `PID_KP_SMALL_ERR` 和 `PID_KD_SMALL_ERR`
3. 检查传感器是否居中安装

### 7.2 转弯时冲出赛道

1. 减小 `SPEED_CURVE`
2. 增大 `PID_KP_MID_ERR` 和 `PID_KP_LARGE_ERR`
3. 检查电池电压是否充足

### 7.3 脱线后无法恢复

1. 检查 `LOST_RECOVERY_SPEED_L` 和 `LOST_RECOVERY_SPEED_R` 方向是否正确
2. 增大 `LOST_RECOVERY_TIME`
3. 检查传感器高度是否合适

### 7.4 速度突变导致抖动

1. 减小 `SPEED_MAX_CHANGE`
2. 检查电机驱动是否正常

## 八、高级功能

### 8.1 标记处理

在 `tracking_handle_mark()` 函数中添加自定义动作：
```c
void tracking_handle_mark(TrackMark_t mark)
{
    switch (mark) {
        case MARK_5X5:
            // 5x5标记：计数或触发动作
            beep_on_times(1, 50);
            break;
        case MARK_15X15:
            // 15x15标记：减速准备作业
            car_set(8, 8);
            break;
        // ... 其他标记
    }
}
```

### 8.2 多速度模式

可以定义多套参数，根据赛道段切换：
```c
typedef struct {
    int16_t kp, ki, kd;
    uint8_t speed_straight, speed_curve, speed_slow;
} Tracking_Profile_t;

Tracking_Profile_t fast_profile = {20, 0, 60, 30, 22, 15};
Tracking_Profile_t stable_profile = {35, 2, 80, 20, 15, 10};
```

## 九、性能优化建议

1. **降低控制周期**：当前主循环周期约10-20ms，如需更高响应速度，可将巡线逻辑放入定时器中断
2. **传感器校准**：不同环境光线下，可添加传感器阈值自动校准
3. **电池电压补偿**：根据电池电压动态调整PID参数

## 十、版本历史

- v1.0 (2024-XX-XX): 初始版本，实现基础巡线功能
- v1.1 (2024-XX-XX): 添加自适应PID和脱线恢复

---

如有问题，请检查：
1. 所有传感器是否正常工作
2. 电机转向是否正确
3. PID参数是否适合当前赛道
