# KSP-32 履带车机械臂编码器四路巡线版 - 源代码说明文档

## 一、项目概述

### 1.1 基本信息
- **项目名称**：KSP-32控制板
- **作者**：XSJ
- **所属公司**：星甲智能
- **创建日期**：2022年05月23日
- **单片机型号**：STM32F103C8T6
- **主频**：72MHz

### 1.2 主要功能
1. PS2手柄按钮控制0-5号舵机，摇杆控制6-7号电机
2. zide图形化编程控制舵机
3. 可脱机存储控制（动作组存储与回放）
4. 四路循迹巡线功能
5. 逆运动学算法支持机械臂控制
6. 多种智能传感器模式（循迹、避障、颜色识别、跟随等）

---

## 二、硬件引脚分配

### 2.1 传感器引脚
| 传感器 | 引脚 | 说明 |
|--------|------|------|
| 循迹S2 | SD2-X2, SA2-X1 (PA0, PA1) | 红外循迹传感器（左2路） |
| 循迹S4 | SD4-X4, SA4-X3 (PA3, PB1) | 红外循迹传感器（右2路） |
| 超声波S3 | PB0(Trig), PA2(Echo) | 超声波测距 |
| 声音S5 | SD5-PA6 | 声音传感器 |
| 颜色识别S6 | PA7, PA5 | TCS34725颜色传感器(I2C) |

### 2.2 舵机引脚
| 舵机 | 引脚 |
|------|------|
| DJ0 | PB3 |
| DJ1 | PB8 |
| DJ2 | PB9 |
| DJ3 | PB6 |
| DJ4 | PB7 |
| DJ5 | PB4 |

### 2.3 其他引脚
| 功能 | 引脚 | 说明 |
|------|------|------|
| 蜂鸣器 | PB5 | 有源蜂鸣器 |
| LED | PB13 | 工作状态指示灯 |
| 总线接口 | TX3, RX3 | USART3，用于底板总线/蓝牙/LoRa |

### 2.4 PS2手柄引脚
| 信号 | 引脚 |
|------|------|
| PS1-DAT | PA15 |
| PS2-CMD | PA14 |
| PS6-ATT | PA13 |
| PS7-CLK | PA12 |

### 2.5 按键引脚
| 按键 | 引脚 |
|------|------|
| KEY1 | PA8 |
| KEY2 | PA11 |

---

## 三、文件结构说明

### 3.1 核心文件列表

| 文件名 | 类型 | 功能描述 |
|--------|------|----------|
| `z_main.c/h` | 主程序 | 程序入口，系统初始化，主循环调度 |
| `z_global.c/h` | 全局变量 | 全局变量定义，舵机结构体，EEPROM信息结构体 |
| `z_rcc.c/h` | 时钟配置 | 系统时钟初始化，外部晶振倍频到72MHz |
| `z_gpio.c/h` | GPIO配置 | IO口初始化，位带操作宏定义，LED/蜂鸣器/舵机IO控制 |
| `z_delay.c/h` | 延时函数 | 微秒/毫秒级延时函数 |
| `z_type.h` | 类型定义 | 自定义数据类型定义(u8/u16/u32等) |

### 3.2 外设驱动文件

| 文件名 | 类型 | 功能描述 |
|--------|------|----------|
| `z_usart.c/h` | 串口驱动 | USART1/2/3初始化，数据收发，中断处理 |
| `z_timer.c/h` | 定时器驱动 | SysTick、TIM1/2/3/4初始化，PWM输出，舵机控制中断 |
| `z_adc.c/h` | ADC驱动 | ADC初始化，DMA传输，模拟量采集 |
| `z_ps2.c/h` | PS2手柄驱动 | PS2手柄通信协议，按键数据读取 |
| `z_w25q64.c/h` | 存储芯片驱动 | W25Q64 Flash读写，动作组存储 |
| `z_sensor.c/h` | 传感器驱动 | 四路循迹、超声波、声音、颜色识别传感器初始化与数据处理 |
| `z_color.c/h` | 颜色传感器驱动 | TCS34725 I2C通信，RGB/HSL颜色转换 |
| `z_oled_i2c.c/h` | OLED驱动 | 0.96寸OLED I2C显示驱动 |

### 3.3 算法文件

| 文件名 | 类型 | 功能描述 |
|--------|------|----------|
| `z_kinematics.c/h` | 逆运动学算法 | 机械臂逆运动学解算，坐标到舵机角度转换 |

### 3.4 系统文件

| 文件名 | 类型 | 功能描述 |
|--------|------|----------|
| `stm32f10x_conf.h` | 配置头文件 | STM32标准库外设头文件包含配置 |
| `stm32f10x_it.h` | 中断头文件 | 中断处理函数声明 |
| `z_stm32f10x_it.c` | 中断处理 | 系统异常处理框架 |
| `tb_table.h` | 表格头文件 | 预留表格定义文件 |

---

## 四、核心功能详解

### 4.1 系统初始化流程

```
main()
  |
  +-- setup_rcc()          // 时钟初始化（外部晶振倍频到72MHz）
  +-- setup_global()       // 全局变量初始化
  +-- setup_gpio()         // GPIO初始化
  +-- setup_nled()         // 工作LED初始化
  +-- setup_beep()         // 蜂鸣器初始化
  +-- setup_djio()         // 舵机IO初始化
  +-- setup_w25q64()       // Flash存储芯片初始化
  +-- setup_ps2()          // PS2手柄初始化
  +-- setup_systick()      // 系统滴答时钟初始化
  +-- setup_uart1()        // 串口1初始化（115200，用于下载动作组）
  +-- setup_uart3()        // 串口3初始化（115200，用于底板总线）
  +-- setup_others()       // 其他初始化（预存命令执行等）
  +-- setup_dj_timer()     // 定时器2初始化（舵机PWM输出）
  +-- setup_start()        // 启动信号（蜂鸣器LED闪烁）
  +-- setup_interrupt()    // 总中断打开
  +-- setup_sensor()       // 传感器初始化
  +-- setup_kinematics()   // 逆运动学参数初始化（90, 105, 98, 150）
```

### 4.2 主循环任务

```
while(1) {
  loop_nled()          // 工作指示灯闪烁（1秒周期）
  loop_uart()          // 串口数据接收处理
  loop_action()        // 动作组批量执行
  loop_ps2_data()      // PS2手柄数据读取（50ms周期）
  loop_ps2_button()    // PS2按键处理
  loop_ps2_car()       // 小车电机摇杆控制
  loop_monitor()       // 定时保存变量到EEPROM（3秒周期）
  loop_sensor()        // 传感器数据处理
}
```

### 4.3 舵机控制机制

- **舵机数量**：8路（DJ_NUM = 8）
- **PWM周期**：1000us（CYCLE = 1000）
- **控制方式**：TIM2定时器中断，20ms周期扫描
- **位置范围**：500-2490（中位1500）
- **增量控制**：通过`duoji_t`结构体的`inc`字段实现平滑过渡

```c
typedef struct {
    uint8_t  valid;   // 有效标志
    uint16_t aim;     // 执行目标位置
    uint16_t time;    // 执行时间
    float    cur;     // 当前位置值
    float    inc;     // 增量（每次中断累加值）
} duoji_t;
```

### 4.4 PS2手柄控制

#### 按键映射（红灯模式）
| 按键 | 功能 |
|------|------|
| L2 | 舵机5低位动作 |
| R2 | 舵机5高位动作 |
| L1 | 舵机4低位动作 |
| R1 | 舵机4高位动作 |
| 方向键上/下/左/右 | 舵机2/3动作 |
| SE/ST | 舵机复位 |

#### 摇杆控制（麦轮小车）
- **左摇杆X轴**（psx_buf[7]）：横向移动控制
- **左摇杆Y轴**（psx_buf[8]）：前后移动控制
- **右摇杆X轴**（psx_buf[5]）：旋转控制
- **右摇杆Y轴**（psx_buf[6]）：前后移动控制
- **速度范围**：-25 ~ 25（限幅系数0.16）
- **电机通道**：#006（左轮）、#007（右轮）
- **安全机制**：10秒无操作自动关闭摇杆控制

#### 摇杆控制算法
```c
// 四轮麦轮速度解算（仅使用前后左右轮）
car_left1   = ((-(127-pss_rx)+(127-pss_ry)-(127-pss_lx)+(127-pss_ly)))*gear_dw;
car_right1  = ((+(127-pss_rx)+(127-pss_ry)+(127-pss_lx)+(127-pss_ly)))*gear_dw;
```

### 4.5 串口命令协议

| 命令格式 | 功能说明 |
|----------|----------|
| `$DRS!` | 测试连接，返回"hello word!" |
| `$DST!` | 所有舵机停止 |
| `$DST:x!` | 第x个舵机停止 |
| `$RST!` | 单片机重启 |
| `$DGS:x!` | 执行第x个动作 |
| `$DGT:x-y,z!` | 执行第x到y组动作z次 |
| `$DCR:x,y!` | 小车左x、右y轮速度控制 |
| `$DJR!` | 所有舵机复位 |
| `$GETA!` | 获取应答信号 |
| `$SMODE:x!` | 设置智能模式（0-10） |
| `$SMART_STOP!` | 智能模式停止 |
| `#000P1500T1000!` | 单舵机控制（0号舵机到1500位置，用时1000ms） |
| `{#000P1500T1000!#001P1500T1000!}` | 多舵机同时控制 |
| `<G0000#000P1500T1000!>` | 保存动作组（带序号） |

### 4.6 智能传感器模式（AI_mode）

| 模式值 | 功能名称 | 说明 |
|--------|----------|------|
| 0 | AI_xunji_moshi | 四路循迹模式 |
| 1 | AI_shengkong_jiaqu | 声控夹取 |
| 2 | AI_ziyou_bizhang | 自由避障 |
| 3 | AI_yanse_shibie | 颜色识别 |
| 4 | AI_dingju_jiaqu | 定距夹取 |
| 5 | AI_gensui_moshi | 超声波跟随 |
| 6 | AI_xunji_bizhang | 循迹避障 |
| 7 | AI_xunji_shibie | 循迹颜色识别 |
| 8 | AI_xunji_dingju | 循迹定距夹取 |
| 9 | AI_shengkong_xunji | 声控循迹 |
| 10/255 | 停止 | 停止所有智能模式 |

### 4.7 四路循迹逻辑

#### 传感器定义
- **x1()**：PA1 - 左外传感器
- **x2()**：PA0 - 左内传感器
- **x3()**：PA3 - 右内传感器
- **x4()**：PB1 - 右外传感器

#### 循迹状态机
| 状态 | 条件 | 动作 |
|------|------|------|
| 直行 | x2=0, x3=0（中间检测到黑线） | car_set(11, 11) |
| 左转 | x4=0 或 (x2=1, x3=0) | car_set(-15, 17) |
| 右转 | x1=0 或 (x2=0, x3=1) | car_set(17, -15) |
| 停止 | x1=1, x2=1, x3=1, x4=1（全白） | car_set(0, 0) |

#### 循迹状态标志
```c
#define TRACKING_STATUS_FORWARD 1   // 直行
#define TRACKING_STATUS_TURN_LEFT 2 // 左转
#define TRACKING_STATUS_TURN_RIGHT 3 // 右转
#define TRACKING_STATUS_STOP 4      // 停止
```

### 4.8 逆运动学算法

#### 结构体定义
```c
typedef struct {
    float L0, L1, L2, L3;          // 机械臂四段长度（单位：0.1mm）
    float servo_angle[6];          // 舵机角度
    float servo_range[6];          // 舵机角度范围
    int servo_pwm[6];              // 舵机PWM值
} kinematics_t;
```

#### 初始化参数
```c
setup_kinematics(90, 105, 98, 150, &kinematics);
// L0=90mm, L1=105mm, L2=98mm, L3=150mm
```

#### 运动学解算
- **输入**：x, y, z坐标 + Alpha夹角
- **输出**：4个舵机的角度和PWM值
- **角度范围**：
  - theta6（旋转）：根据x,y计算
  - theta5（肩关节）：0-180度
  - theta4（肘关节）：-135到135度
  - theta3（腕关节）：-90到90度

#### 错误码
| 返回值 | 说明 |
|--------|------|
| 0 | 成功 |
| 1 | z坐标超出范围 |
| 2 | 目标点超出机械臂工作空间 |
| 3 | acos参数超出范围 |
| 4 | theta5角度超出范围 |
| 5 | acos参数超出范围 |
| 6 | theta4角度超出范围 |
| 7 | theta3角度超出范围 |

### 4.9 存储系统（W25Q64）

- **芯片型号**：W25Q64（8MB Flash）
- **扇区大小**：4KB
- **动作组存储**：每个动作组128字节（ACTION_SIZE = 0x80）
- **配置信息存储地址**：`W25Q64_INFO_ADDR_SAVE_STR` = (8*1024-2)*1024
- **存储内容**：
  - 版本号
  - 舵机偏差值（8路）
  - 预存命令（128字节）
  - 颜色基准值

---

## 五、数据结构

### 5.1 EEPROM信息结构体

```c
typedef struct {
    u32 version;                // 版本号（当前：20180705）
    u32 dj_record_num;          // 学习动作组数量
    u8  pre_cmd[PRE_CMD_SIZE+1];// 预存命令（129字节）
    int dj_bias_pwm[DJ_NUM+1];  // 舵机偏差值（9个，最后一个是校验标志）
    u8 color_base_flag;         // 颜色基准标志
    int color_red_base;         // 红色基准值
    int color_grn_base;         // 绿色基准值
    int color_blu_base;         // 蓝色基准值
} eeprom_info_t;
```

### 5.2 全局变量

| 变量名 | 类型 | 说明 |
|--------|------|------|
| `duoji_doing[8]` | duoji_t | 8路舵机状态数组 |
| `eeprom_info` | eeprom_info_t | EEPROM配置信息 |
| `uart_receive_buf[1024]` | u8 | 串口接收缓冲区 |
| `cmd_return[1024]` | u8 | 命令返回缓冲区 |
| `AI_mode` | u8 | 当前智能模式 |
| `group_do_ok` | u8 | 动作组执行完成标志 |
| `psx_buf[9]` | u8 | PS2手柄数据缓冲区 |
| `kinematics` | kinematics_t | 逆运动学参数 |
| `is_tracking_updated` | u8 | 循迹强制执行标志位 |
| `flagSoundStart` | u8 | 声控启动标志 |

---

## 六、关键宏定义

### 6.1 系统配置
```c
#define VERSION         20180705    // 版本号
#define ACTION_USE_ROM  0           // 0:使用上位机下载动作组
#define CYCLE           1000        // PWM周期
#define DJ_NUM          8           // 舵机数量
#define PSX_BUTTON_NUM  16          // PS2按键数量
#define FLAG_VERIFY     0x25        // 校验标志
#define ACTION_SIZE     0x80        // 动作组存储大小（128字节）
```

### 6.2 PS2模式
```c
#define PS2_LED_RED     0x73        // 红灯模式
#define PS2_LED_GRN     0x41        // 绿灯模式
```

### 6.3 颜色传感器基准值
```c
#define COLOR_RED       215         // 红色基准
#define COLOR_GRN       310         // 蓝色基准
#define COLOR_BLU       283         // 绿色基准
```

---

## 七、中断系统

| 中断源 | 优先级 | 功能 |
|--------|--------|------|
| SysTick | - | 系统滴答时钟，1ms递增 |
| TIM2 | 抢占1/从占2 | 舵机PWM输出控制 |
| USART1 | 抢占1/从占1 | 串口1接收中断 |
| USART2 | 抢占1/从占2 | 串口2接收中断 |
| USART3 | 抢占1/从占3 | 串口3接收中断 |

---

## 八、开发环境

### 8.1 硬件要求
- STM32F103C8T6最小系统板
- W25Q64 Flash存储芯片
- PS2手柄接收模块
- TCS34725颜色传感器
- 超声波模块
- 四路红外循迹模块
- 声音传感器
- OLED显示屏（可选）
- 机械臂结构（4自由度）

### 8.2 软件依赖
- STM32标准外设库 V3.5.0
- ARM CMSIS库
- Keil MDK / IAR EWARM
- math.h（逆运动学计算）

### 8.3 编译配置
- 目标芯片：STM32F103C8T6
- 时钟源：外部高速晶振（HSE）
- 系统时钟：72MHz（PLL 9倍频）
- APB1时钟：36MHz
- APB2时钟：72MHz

---

## 九、注意事项

1. **舵机供电**：舵机数量较多时，请确保电源能够提供足够的电流
2. **Flash写入**：W25Q64写入前需要先擦除，系统已自动处理
3. **串口模式**：串口接收根据首字符自动识别命令类型（`$`命令模式、`#`单舵机、`{`多舵机、`<`保存模式）
4. **偏差校准**：舵机偏差值存储在Flash中，可通过上位机校准
5. **智能模式**：学生可在`z_sensor.c`中的各AI函数内实现自己的逻辑
6. **PS2手柄**：使用JTAG禁用模式（GPIO_Remap_SWJ_JTAGDisable），PA15作为普通IO使用
7. **摇杆安全**：10秒无操作自动关闭摇杆控制，防止误操作
8. **循迹传感器**：检测到黑线时电平为LOW(0)，未检测到时为HIGH(1)
9. **逆运动学**：坐标单位是mm，内部计算会放大10倍提高精度
10. **电机控制**：速度范围-25到25，超出会自动限幅

---

## 十、版本历史

| 版本 | 日期 | 说明 |
|------|------|------|
| 20180705 | 2018-07-05 | 初始版本 |
| 20220523 | 2022-05-23 | KSP-32履带车版本发布 |

---

## 十一、联系方式

- **公司**：星甲智能
- **作者**：XSJ

---

*文档生成日期：2026年04月26日*
